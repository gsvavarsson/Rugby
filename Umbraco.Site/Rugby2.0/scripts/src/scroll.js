app.scroll = {
	init: function() {
		var scr1_scrolled = false;
		var hasAnimated = false;

		

		/*$(window).scroll(function(e){
			var cur = $(window).scrollTop();
			var header = $('header');
			if (cur >= 4 && !scr1_scrolled){

				$(header).addClass('scroll1');

				setTimeout(function(){
					$(header).addClass('scroll2');
				},3000)

				setTimeout(function(){
					$(header).addClass('scroll3');
					scr1_scrolled = true;
				},6000)
			}
		});*/
	}
};